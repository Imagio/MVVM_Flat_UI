<UserControl x:Class="Ru.Imagio.View.WorkspaceShellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/FormStyles.xaml"></ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <WrapPanel Orientation="Horizontal">
            <Button Style="{StaticResource PrimaryButton}">Уведомления</Button>
            <Button Style="{StaticResource PrimaryButton}">Документы</Button>
            <Button Style="{StaticResource PrimaryButton}">Справочники</Button>
            <Button Style="{StaticResource PrimaryButton}">Администрирование</Button>
        </WrapPanel>
    </Grid>
    
</UserControl>
